version: '1.0'
name: pipeline-20240718-10
displayName: pms-base_deploy
triggers:
  trigger: auto
  push:
    branches:
      precise:
        - develop
    commitMessages:
      include:
        - ^deploy:.*
stages:
  - name: stage-3b90465c
    displayName: maven构建
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@maven
        name: build_maven
        displayName: Maven 构建
        jdkVersion: '21'
        mavenVersion: 3.9.6
        commands:
          - mvn clean deploy -Dmaven.test.skip=true -s /root/workspace/futurecraftsmen/pms-base/customSettings.xml
        artifacts:
          - name: BUILD_ARTIFACT
            path:
              - ./pms-enterprise/pms-enterprise-service/target
              - ./pms-system/pms-system-service/target
        settings: []
        caches: []
        notify:
          - certificate: a1acb250-f193-013c-1bfe-422e76947380
            events:
              - fail
            content:
              - repository
              - pipeline
              - stage
              - task
              - operator
              - branch
              - detail
            type: dingtalk
        strategy:
          retry: '0'
strategy:
  blocking: true
