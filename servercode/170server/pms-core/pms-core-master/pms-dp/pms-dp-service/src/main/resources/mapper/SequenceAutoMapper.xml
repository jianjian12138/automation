<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.futurecraftsmen.pms.dp.service.dao.SequenceAuto">


    <select id="nextSeq"
            resultType="java.lang.Long">


        INSERT INTO dm_base.sequence_auto (form_Key, sequence_number)
        VALUES (#{key}, 1) ON CONFLICT (form_Key) DO
        UPDATE
            SET sequence_number = dm_base.sequence_auto.sequence_number + #{step}
            RETURNING sequence_number;
    </select>


    <select id="queryMax"
            resultType="java.lang.Long">
        select sequence_number
        from dm_base.sequence_auto
        where form_Key = #{key}
    </select>


    <update id="updateSeq">
        update dm_base.sequence_auto
        set sequence_number = #{num}
        where form_Key = #{key}
          and #{num} > sequence_number
    </update>

    <update id="updateSeqRest">
        update dm_base.sequence_auto
        set sequence_number = #{num}
        where form_Key = #{key}
    </update>


</mapper>