<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.futurecraftsmen.pms.dm.service.dao.EnterpriseMenuDao">
    <insert id="addEnterpriseMenuModel">
        INSERT INTO dm_m.t_dm_m_enterprise_menu (menu_code, enterprise_code, menu_list,department_config)
        VALUES (#{enterpriseMenuModel.menuCode}, #{enterpriseMenuModel.enterpriseCode}, #{enterpriseMenuModel.menuList},#{enterpriseMenuModel.departmentConfig})
    </insert>

    <update id="updateEnterpriseMenuModel">
        UPDATE dm_m.t_dm_m_enterprise_menu
        SET delete_flag = false
        <if test="enterpriseMenuModel.menuList != null and enterpriseMenuModel.menuList != ''">
            , menu_list = #{enterpriseMenuModel.menuList}
        </if>
        WHERE menu_code = #{enterpriseMenuModel.menuCode}
    </update>

    <update id="updateEnterpriseMenuDepartmentConfig">
        UPDATE dm_m.t_dm_m_enterprise_menu
        SET delete_flag = false
        <if test="enterpriseMenuModel.menuList != null and enterpriseMenuModel.menuList != ''">
            , menu_list = #{enterpriseMenuModel.menuList}
        </if>
        <if test="enterpriseMenuModel.departmentConfig != null and enterpriseMenuModel.departmentConfig != ''">
            , department_config = #{enterpriseMenuModel.departmentConfig}
        </if>
        WHERE menu_code = #{enterpriseMenuModel.menuCode}
    </update>

    <select id="getEnterpriseMenuModelByCodes"
            resultType="com.futurecraftsmen.pms.dm.service.po.EnterpriseMenuModel">
        SELECT id,
        menu_code,
        enterprise_code,
        menu_list,
        department_config,
        update_time,
        create_time,
        delete_flag
        FROM dm_m.t_dm_m_enterprise_menu
        WHERE menu_code IN
        <foreach collection="menuCodes" index="index" item="menuCode" open="(" separator="," close=")">
            #{menuCode}
        </foreach>
        AND delete_flag = false
    </select>

    <select id="getAllEnterpriseMenus" resultType="com.futurecraftsmen.pms.dm.service.po.EnterpriseMenuModel">
        SELECT id,
               menu_code,
               enterprise_code,
               menu_list,
               department_config,
               update_time,
               create_time,
               delete_flag
        FROM dm_m.t_dm_m_enterprise_menu
        WHERE delete_flag = false
    </select>
</mapper>