<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.futurecraftsmen.pms.dm.service.dao.ProductSeriesDao">
    <resultMap id="productSeriesModel" type="com.futurecraftsmen.pms.dm.service.po.base.ProductSeriesModel">
        <result column="primary_key" property="productSeriesCode"/>
        <result column="enterprise_code" property="enterpriseCode"/>
        <result column="product_series_sign" property="productSeriesName"/>
        <result column="superior" property="superior"/>
        <result column="synchronize_status" property="synchronizeStatus"/>
        <result column="modified_times" property="changeCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="delete_flag" property="deleteFlag"/>
    </resultMap>

    <update id="lockSyncData">
        UPDATE ${tableName}
        SET synchronize_status = 4
        WHERE primary_key IN (SELECT primary_key
        FROM ${tableName}
        WHERE enterprise_code = #{enterpriseCode}
        AND synchronize_status &lt;&gt; 1
        AND primary_key IN
        <foreach collection="codes" index="index" item="productSeriesCode" open="(" separator="," close=")">
            #{productSeriesCode}
        </foreach>);
    </update>

    <select id="getLockedSyncData" resultMap="productSeriesModel">
        SELECT primary_key,
               enterprise_code,
               product_series_sign,
               superior,
               synchronize_status,
               modified_times,
               create_time,
               update_time,
               delete_flag
        FROM ${tableName}
        WHERE enterprise_code = #{enterpriseCode}
          AND synchronize_status = 4
    </select>

    <update id="releaseLockedData">
        UPDATE ${tableName}
        SET synchronize_status = 1,
            modified_times     = 0
        WHERE enterprise_code = #{enterpriseCode}
          AND synchronize_status = 4
    </update>

    <select id="getAllData" resultMap="productSeriesModel">
        SELECT primary_key,
               enterprise_code,
               product_series_sign,
               superior,
               synchronize_status,
               modified_times,
               create_time,
               update_time,
               delete_flag
        FROM ${tableName}
        WHERE enterprise_code = #{enterpriseCode}
          AND delete_flag = false
    </select>
</mapper>