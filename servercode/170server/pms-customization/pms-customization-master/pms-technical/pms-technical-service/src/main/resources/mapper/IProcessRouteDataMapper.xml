<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.futurecraftsmen.pms.technical.service.dao.technical.IProcessRouteDataMapper">


    <resultMap id="resultMap" type="com.futurecraftsmen.pms.technical.service.domain.technical.process.ProcessRouteDataModel">
        <result column="primary_key" property="processRouteDataCode"/>
        <result column="type" property="type"/>
        <result column="process_Route_Data_Sign" property="processRouteDataSign"/>
        <result column="remark" property="remark"/>
        <result column="state" property="state"/>
        <result column="enterprise_code" property="enterpriseCode"/>
        <result column="data_version" property="dataVersion"/>
        <result column="delete_flag" property="deleteFlag" />
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="unique_id" property="uniqueId"/>
        <result column="temp_procedure_code" property="tempProcedureCode"/>

        <result column="product_part_code" property="productPartCode"/>
    </resultMap>

    <select id="selectByProcedureCodeIn" resultMap="resultMap">
        select a.* ,b.procedure_code temp_procedure_code
        from ${tableName} a  ,${procedureRouteRelationship} b
        where a.delete_flag = false
        and a.enterprise_code = #{enterpriseCode}
        AND a.delete_flag = 'f'
        and  a.primary_key=b.process_route_data_code
        and b.enterprise_code = #{enterpriseCode}
        AND b.delete_flag = 'f'
        and b.procedure_code in
        <foreach collection="ids" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>


    </select>


    <select id="selectByPartCodeIn" resultMap="resultMap">
        select a.* ,b.unique_id ,c.procedure_code temp_procedure_code  ,b.product_part_code
        from ${tableName} a  ,${productPartRouteRelationship} b ,${gxToGy} c
        where a.delete_flag = false
        and a.enterprise_code = #{enterpriseCode}
        AND a.delete_flag = 'f'
        and  a.primary_key=b.process_route_data_code
        and b.enterprise_code = #{enterpriseCode}
        AND b.delete_flag = 'f'
        and b.product_part_code in
        <foreach collection="ids" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        and a.state=true
        and c.delete_flag = 'f'  and c.enterprise_code = #{enterpriseCode}
        and c.process_route_data_code=b.process_route_data_code
        and c.unique_id=b.unique_id
    </select>


</mapper>