<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.futurecraftsmen.pms.technical.service.dao.collaborate.CollaborateOutPartSendOrderMapper">

    <select id="countByOrderNumber" resultType="int">
        SELECT COUNT(id)
        FROM ${tableName}
        WHERE enterprise_code = #{enterpriseCode}
          AND primary_key != #{excludeSendOrderCode}
          AND send_order_number = #{sendOrderNumber}
          AND delete_flag = 'f'
    </select>
</mapper>