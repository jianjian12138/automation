<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) Zhongzao Software Co. LTD 2022-2062 All rights reserved
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.futurecraftsmen.pms.technical.service.dao.IProcessRouteChangeMapper">
    <resultMap id="resultMap" type="com.futurecraftsmen.pms.technical.service.domain.processroute.ProcessRouteChangeModel">
        <id property="primaryKey" column="primary_key" />
        <result property="processRouteCode" column="process_route_code" />
        <result property="operator" column="operator" />
        <result property="operationDate" column="operation_date" />
        <result property="change" column="change" />
        <result property="originalValue" column="original_value" />
        <result property="changeReason" column="change_reason" />
        <result property="ownedEnterpriseCode" column="enterprise_code" />
    </resultMap>

    <sql id="columns">
        primary_key, process_route_code, operator, operation_date, change, original_value, change_reason
    </sql>

    <insert id="add">
        INSERT INTO ${tableName} (primary_key, process_route_code, operator, operation_date, change, original_value, change_reason, enterprise_code)
        VALUES (#{model.primaryKey}, #{model.processRouteCode}, #{model.operator}, #{model.operationDate}, #{model.change}, #{model.originalValue}, #{model.changeReason},
                #{model.ownedEnterpriseCode})
    </insert>

    <select id="page" resultMap="resultMap">
        SELECT <include refid="columns" />
        FROM ${tableName}
        WHERE enterprise_code = #{enterpriseCode}
            AND process_route_code = #{processRouteCode}
            AND delete_flag = false
        ORDER BY create_time DESC
    </select>


</mapper>