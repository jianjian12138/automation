<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.futurecraftsmen.pms.technical.service.dao.purchase.IPurchaseContractMapper">

    <update id="updateHasReceivingDetails">
        UPDATE ${tableName}
        SET has_receiving_details = 't'
        WHERE
            enterprise_code= #{enterpriseCode}
          AND delete_flag= 'f'
          AND primary_key IN
        <foreach collection="contractCodes" index="index" item="contractCode" open="(" separator="," close=")">
            #{contractCode}
        </foreach>
    </update>

    <select id="validateContractCodes">
        SELECT primary_key
        FROM ${tableName}
        WHERE enterprise_code = #{enterpriseCode}
        AND delete_flag = false
        AND primary_key IN
        <foreach collection="contractCodes" index="index" item="contractCode" open="(" separator="," close=")">
            #{contractCode}
        </foreach>
    </select>


</mapper>