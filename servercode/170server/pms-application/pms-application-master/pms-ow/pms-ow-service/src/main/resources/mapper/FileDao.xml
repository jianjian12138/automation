<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.futurecraftsmen.pms.ow.service.dao.FileDao">
    <resultMap id="FileModel" type="com.futurecraftsmen.pms.ow.service.model.FileModel">
        <result property="fileId" column="file_id"/>
        <result property="enterpriseCode" column="enterprise_code"/>
        <result property="fileUrl" column="file_url"/>
        <result property="location" column="location"/>
        <result property="fileName" column="file_name"/>
        <result property="fileType" column="file_type"/>
        <result property="updateTime" column="update_time"/>
        <result property="createTime" column="create_time"/>
        <result property="deleteFlag" column="delete_flag"/>
    </resultMap>

    <insert id="batchAddFile">
        INSERT INTO ${tableName} (file_id, enterprise_code, file_url, location, file_name, file_type) VALUES
        <foreach collection="fileModels" index="index" item="file" separator=",">
            (#{file.fileId}, #{file.enterpriseCode}, #{file.fileUrl}, #{file.location}, #{file.fileName},
            #{file.fileType})
        </foreach>
    </insert>

    <delete id="batchDeleteFile">
        DELETE FROM ${tableName}
        WHERE file_id IN
        <foreach collection="fileIds" item="fileId" open="(" close=")" separator=",">
            #{fileId}
        </foreach>
        and enterprise_code = #{enterpriseCode}
    </delete>

    <select id="getAllFile" resultMap="FileModel">
        SELECT file_id,
               enterprise_code,
               file_url,
               location,
               file_name,
               file_type,
               update_time,
               create_time,
               delete_flag
        FROM ${tableName}
        WHERE delete_flag = false
          AND enterprise_code = #{enterpriseCode}
    </select>

    <select id="getFilesByCodes" resultMap="FileModel">
        SELECT file_id,
        enterprise_code,
        file_url,
        location,
        file_name,
        file_type,
        update_time,
        create_time,
        delete_flag
        FROM ${tableName}
        WHERE delete_flag = false
        AND file_id IN
        <foreach collection="fileIds" index="index" item="fileId" open="(" separator="," close=")">
            #{fileId}
        </foreach>
    </select>
</mapper>