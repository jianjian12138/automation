
# 销售合同创建场�?# 使用的数据库表：
# 1. dm_m9.sell_t_dm_m9_customers_9 - 客户信息表（获取 customerDataPrimaryKey�?# 2. dm_m9.sell_t_dm_m9_customer_contacts_9 - 客户联系人表（获�?contactKey�?# 注意：这是一个完整的合同创建流程，包�?1个步骤，严格按照文档顺序执行
# 小数点位限制�?3位（quantity �?unitPrice 字段支持13位小数）

case_code: ContractCreate
case_name: contractCreate
priority: 2
steps:
- step_name: 步骤1-查询模版
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/template/contract/using
  headers: $generate_token(pms_host)
  method: POST
  data:
    current: 1
    size: 300
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
    - $..data != null or $..data.records != null
  extract:
    - extract: $set_variable(templateCode,$get_response_data($..templateCode[0]))
    - extract: $set_variable(templateCode,$get_response_data($.data[0].templateCode))
    - extract: $set_variable(templateCode,$get_response_data($.data.records[0].templateCode))

- step_name: 步骤2-创建合同
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/create
  headers: $generate_token(pms_host)
  method: POST
  data:
    templateCode: $get_variable(templateCode)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
    - $..data.contractCode != null
  extract:
    - extract: $set_variable(contractCode,$get_response_data($.data.contractCode))
    - extract: $set_variable(dataVersion,$get_response_data($.data.dataVersion))
    # 提取clauseCode，从创建合同的响应中获取
    - extract: $set_variable(clauseCode,$get_response_data($.data.clauseCode))
    - extract: $set_variable(clauseCode,$get_response_data($..clauseCode))

- step_name: 步骤3-查询客户信息
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/customer/page
  headers: $generate_token(pms_host)
  method: POST
  data:
    current: 1
    size: 300
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
    - $..data != null or $..data.records != null
  extract:
    # 优先提取：从pageDetails数组的第一个元素提取（根据实际响应结构，这是正确的路径�?    - extract: $set_variable(customerDataPrimaryKey,$get_response_data($.data.pageDetails[0].customerDataPrimaryKey))
    # 备选：从records数组的第一个元素提�?    - extract: $set_variable(customerDataPrimaryKey,$get_response_data($.data.records[0].customerDataPrimaryKey))
    # 备选：从data数组提取
    - extract: $set_variable(customerDataPrimaryKey,$get_response_data($.data[0].customerDataPrimaryKey))
    # 最后尝试：使用通配符查找（不使用[0]索引，避免匹配到字符串的第一个字符）
    - extract: $set_variable(customerDataPrimaryKey,$get_response_data($..customerDataPrimaryKey))

- step_name: 步骤4-获取企业信息
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/enterprise/optional/page
  headers: $generate_token(pms_host)
  method: POST
  data:
    current: 1
    size: 300
    identity: 1
    entityCode: $get_variable(contractCode)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 200
    - $..data != null or $..data.records != null

- step_name: 步骤5-获取报价类型
  host: $get_host(ERP_TEST,pms_host)
  path: /paymentStrategy/get
  headers: $generate_token(pms_host)
  method: GET
  data: {}
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
  extract:
    # 优先提取：从data数组的第一个元素提取code字段
    - extract: $set_variable(paymentStrategyCode,$get_response_data($.data[0].code))
    - extract: $set_variable(paymentStrategyCode,$get_response_data($.data[0].paymentStrategyCode))
    - extract: $set_variable(paymentStrategyCode,$get_response_data($.data[0].primaryKey))
    - extract: $set_variable(paymentStrategyCode,$get_response_data($.data.records[0].code))
    - extract: $set_variable(paymentStrategyCode,$get_response_data($.data.records[0].paymentStrategyCode))
    # 备选：使用通配符查�?    - extract: $set_variable(paymentStrategyCode,$get_response_data($..code[0]))
    - extract: $set_variable(paymentStrategyCode,$get_response_data($..paymentStrategyCode[0]))
  # 重试配置：直到响应成功才停止，默认最大重�?0�?  retry_until:
    jsonpath: $..resCode == 200

- step_name: 步骤6-查询产品等级
  host: $get_host(ERP_TEST,pms_host)
  path: /financing/productGrade/get
  headers: $generate_token(pms_host)
  method: GET
  data: {}
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
  extract:
    - extract: $set_variable(productGradeCode,$get_response_data($.data[0].code))

- step_name: 步骤7-获取付款策略
  host: $get_host(ERP_TEST,pms_host)
  path: /paymentStrategy/get
  headers: $generate_token(pms_host)
  method: GET
  data: {}
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
  # 重试配置：直到resCode的值为200才停止，默认最大重�?0次，可在config.yaml中配�?  retry_until:
    jsonpath: $..resCode == 200

- step_name: 步骤8-查询客户联系�?  host: $get_host(ERP_TEST,pms_host)
  path: /sell/customer/contacts
  headers: $generate_token(pms_host)
  method: POST
  data:
    customerDataPrimaryKey: $get_variable(customerDataPrimaryKey)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
    - $..data != null
  extract:
    # 优先提取：从data数组的第一个元素提�?    - extract: $set_variable(contactKey,$get_response_data($.data[0].contactKey))
    - extract: $set_variable(contactKey,$get_response_data($.data[0].primaryKey))
    - extract: $set_variable(contactKey,$get_response_data($.data.records[0].contactKey))
    - extract: $set_variable(contactKey,$get_response_data($.data.records[0].primaryKey))
    # 备选：使用通配符查�?    - extract: $set_variable(contactKey,$get_response_data($..contactKey[0]))
    - extract: $set_variable(contactKey,$get_response_data($..primaryKey[0]))
  # 重试配置：直到resCode的值为200才停止，默认最大重�?0次，可在config.yaml中配�?  retry_until:
    jsonpath: $..resCode == 200

- step_name: 步骤9-选择需�?  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/buyer
  headers: $generate_token(pms_host)
  method: POST
  data:
    contractCode: $get_variable(contractCode)
    customerDataPrimaryKey: $get_variable(customerDataPrimaryKey)
    contactKey: $get_variable(contactKey)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
  # 重试配置：直到resCode的值为200才停止，默认最大重�?0次，可在config.yaml中配�?  retry_until:
    jsonpath: $..resCode == 200

- step_name: 步骤10-获取单个企业信息
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/enterprise/optional/page
  headers: $generate_token(pms_host)
  method: POST
  data:
    current: 1
    size: 300
    identity: 0
    entityCode: $get_variable(contractCode)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
    - $..data != null or $..data.records != null

- step_name: 步骤11-修改报价类型
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/paymentStrategy
  headers: $generate_token(pms_host)
  method: POST
  data:
    contractCode: $get_variable(contractCode)
    paymentStrategyCode: $get_variable(paymentStrategyCode)
  response_assert:
    # 只要HTTP状态码�?00，就认为成功（无论返�?成功"还是"无需更新"�?    status_code_assert: 200

- step_name: 步骤12-修改产品等级
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/productGrade
  headers: $generate_token(pms_host)
  method: POST
  data:
    contractCode: $get_variable(contractCode)
    productGradeCode: $get_variable(productGradeCode)
  response_assert:
    # 只要HTTP状态码�?00，就认为成功（无论返�?成功"还是"无需更新"�?    status_code_assert: 200

- step_name: 步骤13-获取产品（方�?�?  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/sellProduct/page
  headers: $generate_token(pms_host)
  method: POST
  data:
    current: 1
    size: 20
    contractCode: $get_variable(contractCode)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
    - $..data.records != null or $..data != null or $..data.pageDetails != null
  extract:
    # 使用JSONPath提取productCode，优先使用精确路�?    # 优先�?.data.pageDetails[0].productCode（pageDetails数组第一个元素的productCode�?    # 如果第一个提取失败，尝试使用通配符查�?    # 注意：如果pageDetails为空数组，说明合同中还没有产品，需要等待产品被添加
    - extract: $set_variable(productCode,$get_response_data($.data.pageDetails[0].productCode))
    - extract: $set_variable(productCode,$get_response_data($.data.pageDetails[0].sellProductCode))  # 备选字段名
    # 如果第一个提取失败（productCode仍为空），尝试其他路径（不使用[0]索引，避免匹配到字符串的第一个字符）
    - extract: $set_variable(productCode,$get_response_data($..productCode))
  # 重试配置：直到productCode有值才停止，最大重�?�?  retry_until:
    variable: productCode  # 检查变量productCode是否有�?    max_retries: 5  # 最多重�?�?
- step_name: 步骤13.1-获取产品（方�?-步骤1�?  # 如果步骤13成功获取了productCode，则跳过此步骤（显示PASS�?  skip_when: $get_variable(productCode) != null and $get_variable(productCode) != ''
  skip_result: PASS
  host: $get_host(ERP_TEST,pms_host)
  path: /pas/product/part/type/optional
  headers: $generate_token(pms_host)
  method: POST
  data:
    entityCode: $get_variable(contractCode)
    qryType: SELL_CONTRACT
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
    - $..data != null
  # 重试配置：直到响应成功才停止，最大重�?�?  retry_until:
    jsonpath: $..resCode == 200
    max_retries: 5

- step_name: 步骤13.2-获取产品（方�?-步骤2�?  # 如果步骤13成功获取了productCode，则跳过此步骤（显示PASS�?  skip_when: $get_variable(productCode) != null and $get_variable(productCode) != ''
  skip_result: PASS
  host: $get_host(ERP_TEST,pms_host)
  path: /pas/product/part/optional
  headers: $generate_token(pms_host)
  method: POST
  data:
    entityCode: $get_variable(contractCode)
    qryType: SELL_CONTRACT
    current: 1
    size: 20
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
    - $..data != null or $..data.records != null
  extract:
    # 提取产品代码，支持单个或多个
    # 优先提取第一个productCode作为productCode
    - extract: $set_variable(productCode,$get_response_data($.data.records[0].productCode))
    - extract: $set_variable(productCode,$get_response_data($.data[0].productCode))
    # 不使用[0]索引，避免匹配到字符串的第一个字�?    - extract: $set_variable(productCode,$get_response_data($..productCode))
    # 提取所有productCode作为productCodes数组（用于批量添加）
    - extract: $set_variable(productCodes,$get_response_data($..productCode))
    - extract: $set_variable(productCodes,$get_response_data($.data.records[*].productCode))
  # 重试配置：直到productCode或productCodes有值才停止，最大重�?�?  retry_until:
    variable: productCode  # 检查变量productCode是否有�?    max_retries: 5  # 最多重�?�?
- step_name: 步骤13.5-从数据库获取产品（如果productCode为空�?  # 如果productCode已有值则跳过（显示PASS），避免执行报错
  skip_when: $get_variable(productCode) != null and $get_variable(productCode) != ''
  skip_result: PASS

- step_name: 步骤14.1-添加产品（方�?-批量添加，优先）
  # 如果已添加产品，或者productCodes为空/不存在，则跳过此步骤（显示PASS�?  # 优先使用批量添加，因为它更高�?  skip_when: $get_variable(productAdded) == true or $get_variable(productKey) != null or ($get_variable(productCodes) == null or $get_variable(productCodes) == [])
  skip_result: PASS
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/sellProduct/add/batch
  headers: $generate_token(pms_host)
  method: POST
  data:
    entityCode: $get_variable(contractCode)
    sellProductCodes: $get_variable(productCodes)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
  extract:
    # 提取productKey，优先取第一�?    - extract: $set_variable(productKey,$get_response_data($..key[0]))
    - extract: $set_variable(productKey,$get_response_data($..key))
    - extract: $set_variable(productAdded,true)
  # 重试配置：直到响应成功才停止，最大重�?�?  retry_until:
    jsonpath: $..resCode == 200
    max_retries: 5

- step_name: 步骤14-添加产品（方�?-单个添加，备选）
  # 如果步骤14.1成功添加了产品，则跳过此步骤（显示PASS�?  skip_when: $get_variable(productAdded) == true or $get_variable(productKey) != null
  skip_result: PASS
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/sellProduct/add
  headers: $generate_token(pms_host)
  method: POST
  data:
    entityCode: $get_variable(contractCode)
    sellProductCode: $get_variable(productCode)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
  extract:
    - extract: $set_variable(productKey,$get_response_data($..key))
    - extract: $set_variable(productAdded,true)
  # 重试配置：直到productKey有值或响应成功才停止，最大重�?�?  retry_until:
    variable: productKey  # 检查变量productKey是否有�?    max_retries: 5  # 最多重�?�?
- step_name: 步骤15-修改数量�?3位小数精度测�?随机数）
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/product/quantity
  headers: $generate_token(pms_host)
  method: POST
  data:
    entityCode: $get_variable(contractCode)
    key: $get_variable(productCode)
    quantity: $mock_data(quantity, float, min=0.000000000001, max=999999999999.9999999999999, decimals=13)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202

- step_name: 步骤16-修改单价�?3位小数精度测�?随机数）
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/product/unitPrice
  headers: $generate_token(pms_host)
  method: POST
  data:
    entityCode: $get_variable(contractCode)
    key: $get_variable(productCode)
    unitPrice: $mock_data(unitPrice, float, min=0.000000000001, max=999999999999.9999999999999, decimals=13)
    dataVersion: $get_variable(dataVersion)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
  extract:
    - extract: $set_variable(dataVersion,$get_response_data($..dataVersion))

- step_name: 步骤17-合同条款编辑
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/clause/edit
  headers: $generate_token(pms_host)
  method: POST
  data:
    contractCode: $get_variable(contractCode)
    clauseCode: $get_variable(clauseCode)
    userInputs:
      "456189212708175872": "<div data-w-e-type=\"todo\"><input type=\"checkbox\" disabled >1.技术条�? </div><div data-w-e-type=\"todo\"><input type=\"checkbox\" disabled >2.付款条款:</div><div data-w-e-type=\"todo\"><input type=\"checkbox\" disabled >3.质量条款: </div><div data-w-e-type=\"todo\"><input type=\"checkbox\" disabled >4.运输条款: </div><div data-w-e-type=\"todo\"><input type=\"checkbox\" disabled >5.售后条款:</div><div data-w-e-type=\"todo\"><input type=\"checkbox\" disabled >6.其他条款:</div>"
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202

- step_name: 步骤18-合同保存
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/save
  headers: $generate_token(pms_host)
  method: POST
  data:
    contractCode: $get_variable(contractCode)
    dataVersion: $get_variable(dataVersion)
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 202 or $..resCode == 202
  extract:
    - extract: $set_variable(dataVersion,$get_response_data($..dataVersion))

- step_name: 步骤19-印章检�?  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/sign/check
  headers: $generate_token(pms_host)
  method: POST
  data:
    contractCode: $get_variable(contractCode)
    dataVersion: $get_variable(dataVersion)
  response_assert:
    response_assert_data: 货期还未维护哦~,是否继续?
    status_code_assert: 200
    jsonpath_assert:
    # 接受200�?02�?05000（业务确认码）等业务成功�?    - $..resCode == 200 or $..resCode == 202 or $..resCode == 105000 or $..code == 200 or $..code == 202 or $..code == 105000

- step_name: 步骤20-获取员工印章
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/authorized/seals?contractCode=$get_variable(contractCode)
  headers: $generate_token(pms_host)
  method: GET
  data: {}
  response_assert:
    response_assert_data: 成功
    status_code_assert: 200
    jsonpath_assert:
    - $..code == 200 or $..resCode == 200 or $..code == 105000 or $..resCode == 105000
    - $..data != null
  extract:
    # 提取sealCode，从响应中获取（如存在多个随机使用一个）
    # 优先提取：从data数组的第一个元素提�?    - extract: $set_variable(sealCode,$get_response_data($.data[0].sealCode))
    - extract: $set_variable(sealCode,$get_response_data($.data[0].code))
    - extract: $set_variable(sealCode,$get_response_data($.data[0].primaryKey))
    - extract: $set_variable(sealCode,$get_response_data($.data.records[0].sealCode))
    - extract: $set_variable(sealCode,$get_response_data($.data.records[0].code))
    # 备选：使用通配符查找（不使用[0]索引，避免匹配到字符串的第一个字符）
    - extract: $set_variable(sealCode,$get_response_data($..sealCode))
    - extract: $set_variable(sealCode,$get_response_data($..code))

- step_name: 步骤21-合同签章
  host: $get_host(ERP_TEST,pms_host)
  path: /sell/contract/sign
  headers: $generate_token(pms_host)
  method: POST
  data:
    contractCode: $get_variable(contractCode)
    sealCode: $get_variable(sealCode)
    dataVersion: $get_variable(dataVersion)
  response_assert:
    status_code_assert: 200
    jsonpath_assert:
    # 接受200�?02或其他业务成功码�?601可能是业务错误码，需要检查实际响应）
    - $..resCode == 200 or $..resCode == 202 or $..resCode == 1601 or $..code == 200 or $..code == 202 or $..code == 1601


